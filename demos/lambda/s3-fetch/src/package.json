{
  "name": "s3-fetcher",
  "version": "1.0.0",
  "description": "Simple NodeJS lambda to fetch blog content from an S3 bucket",
  "main": "handler.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ../dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jamesdylanconklin@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.857.0"
  }
}
